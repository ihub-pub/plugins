/**
 +----------------+
 | Build Settings |
 +----------------+
 */
subprojects {
    apply {
        plugin 'groovy'
        plugin 'maven'
        from rootProject.file('gradle/publishing_settings.gradle')
    }

    dependencies {
        implementation gradleApi()
    }
}
